---
description: Wire Resend for transactional email — install SDK, scaffold lib/email/, add a sample template, optionally point Supabase auth emails through Resend SMTP.
---

Run the `add-email` skill. Read `.claude/skills/add-email/SKILL.md` and follow it exactly.
