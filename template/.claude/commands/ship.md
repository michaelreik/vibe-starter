---
description: Verify locally, commit pending work, push to main, wait for Vercel, smoke-check the live URL, update PROGRESS.md.
---

Run the `deploy-to-production` skill. Read `.claude/skills/deploy-to-production/SKILL.md`
and follow it exactly. All four pre-flight checks (typecheck, lint, test,
build) must pass before pushing.
