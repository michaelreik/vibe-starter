---
description: Add a new user-facing feature end-to-end (data model, RLS, server actions, UI, tests, progress).
---

Run the `add-feature` skill. Read `.claude/skills/add-feature/SKILL.md` and
follow it exactly. Always read `PROGRESS.md` and `DECISIONS.md` first for
context. Plan out loud before writing code; ask 2-3 clarifying questions;
then execute migration → server actions → UI → tests → progress update.

Feature description from user: $ARGUMENTS
