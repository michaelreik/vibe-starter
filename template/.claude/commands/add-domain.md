---
description: Point a custom domain at Vercel. SSL automatic via LetsEncrypt. Cloudflare-first; supports existing-domain flows.
---

Run the `add-custom-domain` skill. Read `.claude/skills/add-custom-domain/SKILL.md` and follow it exactly. First question: does the user already own a domain?

Domain (if user supplied it): $ARGUMENTS
